# Attention workspace text hierarchy

- Status: active
- Reorientation budget: 120
- Owner: Bokkie attention UI
- Next Action: Qualify the committed consumer, reconcile its evidence and obtain independent review.
- Framework owner: [Polyorama #30](https://github.com/robchristie/polyorama/pull/30), `d10b6864ef278fe98fa927111f97d6d142344aab`
- Starting Bokkie revision: `b2575d6`
- Framework dependency: [Polyorama text hierarchy plan](https://github.com/robchristie/polyorama/blob/5055c21b5b6085f2ec9396d6f9c3306e378b9d14/docs/text-hierarchy-plan.md)

## Outcome

Make the attention workspace readable in task order, adopt real semantic
hierarchy, derive virtual row geometry from the font/density recipe, and replace
the invalid 24-line evidence label with a selectable scrolling reader. Preserve
backend capability authority, stale-state disabling and exact confirmations.

## Current phase

Consumer qualification against the reviewed, merged framework owner.
Both canonical backend and UI checks pass against the final Git pin. The consumer uses the
reading profile and deliberately fixed row slots; current legal actions precede
technical disclosures. No production data or deployment is involved.

Calibration question: can the normal and enlarged reading scale retain compact
rows and a usable action-first detail surface while complete evidence remains
available? The smallest probe uses the existing deterministic full fixture at
1440×900 and 1280×720, the row-geometry tests at 100% and 150%, and the long
completed-event fixture with unique marker `BOKKIE_EVIDENCE_TAIL_7F39`.
The application and browser qualification evidence own the result. Exit requires
clean semantic/text audits, physically visible tail content after inner scrolling,
canonical native/WASM verification and a coherent reviewed candidate.

## Acceptance

- Reading roles resolve through the framework's bundled regular/semibold faces.
- Virtualised obligation rows fit at 100% and 150%, in both supported densities.
- Details answer what needs attention and what happens next before identifiers.
- Only legal actions appear; stale/in-flight decisions remain disabled.
- Exact mandatory gardener provenance remains in every confirmation.
- The long evidence fixture's tail is actually painted after physical scrolling.
- Failed layout attempts cannot disappear through the offscreen audit filter.
- Canonical backend/UI checks and native/browser qualification pass.

## Evidence

Focused component tests and browser journeys are checked in with their semantic
owners. Disposable fixture outputs belong under ignored `target/text-hierarchy`.
Provisional calibration retained the consumer candidate on Bokkie base
`b2575d64776feefe4541c5e7db9851ee553afa0d`, with app source SHA-256
`805f03d82f4f41ec9acd818eaff95eea0d9275361dd6a28c776bb500c1e9342b`.
The local framework candidate was `d43277231cace515925f642003a87f68caf4488a`.
The synthetic fixture source SHA-256 was
`2b555102373a4613b1d3e838eed36b88149106f85df065a325a592b71dae1fe2`.
All 42 focused UI tests and strict Clippy passed. The actual browser passed seven
journeys with no unexpected errors, clean audits and idle frames 158 → 158.
Physical virtual-ledger selection, disclosure and inner scrolling exposed the
unique tail marker in the painted galley and inspected screenshot. The probe
rejected a reader that could shrink to 64 points and retained a deliberate
12-line minimum viewport. Full fixture identities, source/binary hashes, results
and captures are in `target/text-hierarchy/calibration/calibration-identity.json`
and adjacent browser artefacts. The final pinned qualification and review remain
with the delivery coordinator before this plan moves to completed.

## Next action

Run exact-revision native/browser qualification, retain the evidence and complete
independent review and CI before landing.
