# Pagefold supervised milestone

Status: accepted after the recorded infrastructure interventions and full
requalification. [Machine-readable acceptance](pagefold.json) retains final
identities and provenance. The original durable outcome was
`d311030a-ebfc-4495-b60d-0db71ef3fd61`, submitted after fixture j qualified.

## Retained run

The task-scoped evidence owner is
`/nvme/development/pagefold-supervision-20260909-a`: its SQLite database,
`submitted-intent.json`, original profile and `source-before.json`, broker
journals, `journey.json` and `infrastructure-intervention.json` retain the run.
Application source is local-only at `/nvme/development/pagefold`. Synthetic
knowledge is separate under the evidence owner's `knowledge` directory.

Bokkie formalised the intent, delegated reader calibration to a fresh worker,
inspected and accepted that submission, and delegated the implementation to a
second fresh worker. The workers commissioned separate independent reviews.
Calibration found missing Japanese glyphs; Bokkie carried that constraint into
the implementation brief. The implementation worker repaired browser harness,
layout and font-packaging issues within its assigned scope and submitted exact
source and verification evidence. Final UI qualification was subsequently repeated under the continuation below.

## Infrastructure intervention

The original runtime was `381fc8186322e8429e585225c265ffc071b50fd9`.
During implementation assessment, supervisor
`6d3ab99a-ab74-49cc-b84a-02d3479644c6` repeatedly paged lossy binary font output.
Its journal reached 2,046 events and 15,581,478 bytes, recorded exhaustion and
proved namespace reaping. Product acceptance remained pending.

The outer agent stopped the controller and requested the remaining read-only
supervisor `1fcb27dc-2218-4963-8946-4a4917142f48` to stop through its broker.
Its retained boundary receipt proves reaping. No application source was edited,
no worker brief or routine decision was supplied, and the outcome was not
cancelled. The Store must reconcile that proof through its normal runtime path.

The repair makes binary artefact inspection compact and explicit after exact
byte-count and digest verification. Text inspection and review remain complete.
The observation driver gains a checked resume mode preserving the original
outcome, profile, intent, source inventory and deadline. The affected
implementation-acceptance journey must repeat under the repaired runtime,
followed by final UI qualification and product acceptance. This record does
not treat the interrupted attempt as a successful final dogfood run.

The same outcome resumed at `6a0b08ad8bb12b0c973fae0108a83673c9357389`.
The driver verified the original profile hash, intent and source inventory, and
kept the existing deadline. Normal Store reconciliation verified the stopped
supervisor's cessation and charged one recovery within the existing budget;
fresh supervisor `73791f25-717c-426d-944f-618be7bc0388` owns the repeated
implementation assessment. No routine decision was supplied by the outer agent.

That repeated assessment succeeded: Bokkie accepted the implementation and
commissioned UI qualification in worker `6da0c3a6-bb3c-455a-b6eb-0551dea97712`.
The worker completed browser journeys and verification, but its final source
inventory exceeded the adapter's 16 MiB aggregate capture limit by 100,686 bytes.
Supervisor `1d7a099e-a6e0-44b8-b970-76d7546cddcd` diagnosed the limit from
recorded evidence and raised a precise missing-information escalation because
adapter changes lay outside its application scope. The outer agent did not
answer the routine worker question or alter the application.

The observation driver's failure cleanup incorrectly cancelled this recoverable
attempt. All eleven execution boundaries were reconciled; no writer remains.
This is a second infrastructure failure, not a successful product acceptance.
The repair separates the 32 MiB source-capture limit from the unchanged 16 MiB
journal limit, retains per-file/count limits and exact evidence binding, and
changes observer interruption to preserve outcomes and pending reconciliation.
A replacement intake must retain the original intent and application evidence,
use only the remaining consumable budgets, and keep absolute deadline
`1788957904`. It requires proven cessation of every prior execution. The new
supervisor must requalify the deliverable; prior summaries confer no acceptance.

The repair passed independent review at
`c156da91eafc6097b9bd6d1beea657ace9db6b3d`. Continuation intake is now
`20a8e12c-7e60-46c9-8b27-bc8d3ce61d20`, linked to the cancelled predecessor in
`journey.json`. Its exact profile SHA-256 is
`fd3e7e62ff498376f6a8f84fbd098dd1acf01c47d10553e98a3085fa49c29a1f`;
`infrastructure-interventions/source-capture.json` records the intervention.
The remaining caps are 13 turns, five packages, three repairs and two recoveries;
question/checkpoint consumption and the original absolute deadline are also
preserved. The driver saves an immutable pending intake capsule before HTTP
submission so a lost acknowledgement reuses the same profile and command.
The outer agent remains an observer of this new supervised qualification.

## Accepted continuation and local delivery

Bokkie accepted outcome `20a8e12c-7e60-46c9-8b27-bc8d3ce61d20`, contract 2,
after seven execution turns under the remaining budget. Result
`fa374fa1-30f0-4e3b-96b8-edf38eae4313` came from worker
`26d76e4d-c009-469f-89ed-bf630f1ba85c`; separate supervisor
`7c3ecdb0-2999-44c5-99d2-79a89042d3bd` inspected source, tests, exact review,
command evidence, manifests and opened Lantern images before assessment
`4e217492-1646-4530-a60c-33cdb70eb0a9`. All seven criteria passed. All execution
boundaries are reconciled and the original five synthetic files are unchanged.

Bokkie's workers added an index-corruption regression and repaired a real
independent-review finding: navigation after failed refresh could hide the stale
warning. The repaired warning survives links/search and clears only after a
successful refresh. A worker journal exhausted its finite bound; Bokkie proved
cessation before dispatching a fresh worker over retained work. The runtime
rejected changing-source validation, and the worker reran it with stable inputs.
One routine report-size question was persisted and answered by a fresh supervisor
from runtime source, preserving full inspection through exact evidence manifests.
The final continuation needed no outer routine decision, worker instruction,
application source edit or human answer.

The final independent review covers 51 registered artefacts and a verified
157-entry evidence manifest. Canonical Pagefold verification passed again during
closeout: six Python tests, four Rust tests, formatting, Clippy, native/WASM
builds, bindgen and unchanged-source inventory. All accepted file identities
were checked after that run. The outer agent committed those unchanged files
locally as `53118c0a53915a58dc6c57e02cf3c1d24d4b3b9e`, tree
`415bb17fb65a35167ce0a1a6b37699ee7a2ae27c`; there is no remote. Two whitespace
warnings in historical diagnostic logs are retained byte-for-byte. Application
reports deliberately preserve submission-time status; this Bokkie record and
its authoritative database retain subsequent product acceptance.

Polyorama remains unchanged at `fba3db87aaeb4a7a9f1bbfd0ba59fa0aac713b21`.
Pagefold's README describes local use. Its browser service selects an absolute
knowledge directory, browses/renders pages and relative links/images, searches
a rebuildable literal-text index and refreshes external changes. Source files
are read-only; state lives separately; HTML is inert; escaping paths and symlinks
are rejected. No private content, repository publication or deployment occurred.

Qualification uses Chrome/SwiftShader at ordinary and narrow widths. Canvas
accessibility, native interaction and hardware GPU behaviour remain unqualified;
native compilation passed. Narrow DOM width reports include an off-viewport
input, while opened canvas content fits. Logs retain an incidental favicon 404
and the expected failed-refresh 400. Refresh is explicit and scanning is not an
atomic filesystem snapshot. These limits are documented rather than hidden.
