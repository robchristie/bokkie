run_type = "experiment"
executor_labels = ["cpu"]
evaluator_commands = [
  "uv run ruff format --check src tests",
  "uv run ruff check src tests",
  "uv run pytest -q",
]
timeout_seconds = 3600
auto_continue = true
